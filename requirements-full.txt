-r requirements-minimal.txt
# sentence-transformers provides BOTH the bi-encoder (all-MiniLM-L6-v2) and the
# cross-encoder (cross-encoder/ms-marco-MiniLM-L-6-v2) used by the hybrid re-ranker.
sentence-transformers>=3.0
faiss-cpu>=1.8
umap-learn>=0.5
reportlab>=4.2
pytest>=8.0
